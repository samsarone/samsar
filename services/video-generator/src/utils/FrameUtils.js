import fs from 'fs';
import path from 'path';

export async function deleteFramesForGeneration(sessionId) {
  // try {
  //   const pwd = process.cwd();
  //   const folderPath = path.join(pwd, '../', 'samsar_processor', 'assets', 'video', 'frames', sessionId);

  //   // Check if the folder exists
  //   if (fs.existsSync(folderPath)) {
  //     // Delete all files in the folder and the folder itself
  //     fs.rmdirSync(folderPath, { recursive: true });
  //   } else {
  //   }
  // } catch (error) {
  //   console.error(`Error while deleting folder: ${error.message}`);
  // }
}
