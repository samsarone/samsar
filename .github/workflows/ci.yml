name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Unit and deployment tests
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install UI test dependencies
        run: |
          mkdir -p "$RUNNER_TEMP/samsar-client-deps"
          cp apps/samsar-client/package.json apps/samsar-client/yarn.lock "$RUNNER_TEMP/samsar-client-deps/"
          yarn --cwd "$RUNNER_TEMP/samsar-client-deps" install --frozen-lockfile --non-interactive
          ln -s "$RUNNER_TEMP/samsar-client-deps/node_modules" apps/samsar-client/node_modules
          npm ci --prefix apps/setup-wizard --no-audit --no-fund

      - name: Run tests
        run: npm run test:ci

  windows-launcher:
    name: Windows launcher validation
    runs-on: windows-latest
    timeout-minutes: 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Parse the PowerShell launcher
        shell: powershell
        run: |
          $tokens = $null
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile(
            (Resolve-Path ./setup.ps1),
            [ref] $tokens,
            [ref] $errors
          ) | Out-Null
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            exit 1
          }

      - name: Run launcher contract tests
        run: node --test --test-name-pattern="Windows launchers" scripts/setup-launchers.test.mjs
